// service identifiers used for dynamic DI bindings in presentation builders

const PRESENTATION_TYPES = {
  IBottomTooltipPresenter: Symbol.for("IBottomTooltipPresenter"),
};

export default PRESENTATION_TYPES;
