import SpaceController from "../../../../Core/Presentation/Babylon/Spaces/SpaceController";

describe("SpaceController", () => {
  let systemUnderTest: SpaceController;

  beforeEach(() => {
    systemUnderTest = new SpaceController();
  });

  test.todo("write tests when controller has functionality");
});
